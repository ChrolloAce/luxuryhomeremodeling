import { Metadata } from 'next';
import { generateMetadata } from '../utils/metadata-utils';

// Generate metadata for the Contact page
const metadata: Metadata = generateMetadata('contact');

export default metadata; 