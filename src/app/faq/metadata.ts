import { Metadata } from 'next';
import { generateMetadata } from '../utils/metadata-utils';

// Generate metadata for the FAQ page
const metadata: Metadata = generateMetadata('faq');

export default metadata; 