import { Metadata } from 'next';
import { generateMetadata } from '../utils/metadata-utils';

// Generate metadata for the Portfolio page
const metadata: Metadata = generateMetadata('portfolio');

export default metadata; 