import { Metadata } from 'next';
import { generateMetadata } from '../../utils/metadata-utils';

// Generate metadata for the Upholstery Cleaning page
const metadata: Metadata = generateMetadata('services.upholstery');

export default metadata; 