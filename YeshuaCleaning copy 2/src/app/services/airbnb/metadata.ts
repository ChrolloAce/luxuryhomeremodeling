import { Metadata } from 'next';
import { generateMetadata } from '../../utils/metadata-utils';

// Generate metadata for the Airbnb Rental Cleaning page
const metadata: Metadata = generateMetadata('services.airbnb');

export default metadata; 