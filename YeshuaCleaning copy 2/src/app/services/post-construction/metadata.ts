import { Metadata } from 'next';
import { generateMetadata } from '../../utils/metadata-utils';

// Generate metadata for the Post-Construction Cleaning page
const metadata: Metadata = generateMetadata('services.postConstruction');

export default metadata; 