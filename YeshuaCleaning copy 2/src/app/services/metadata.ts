import { Metadata } from 'next';
import { generateMetadata } from '../utils/metadata-utils';

// Generate metadata for the Services page
const metadata: Metadata = generateMetadata('services.main');

export default metadata; 