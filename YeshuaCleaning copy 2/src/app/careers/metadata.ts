import { Metadata } from 'next';
import { generateMetadata } from '../utils/metadata-utils';

// Generate metadata for the Careers page
const metadata: Metadata = generateMetadata('careers');

export default metadata; 